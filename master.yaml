version: '2.1'

services:
    portainer:
        extends:
            file: portainer/portainer.yaml
            service: portainer
    samba:
        extends:
            file: samba/samba.yaml
            service: samba
    sftp:
        extends:
            file: sftp/sftp.yaml
            service: sftp
