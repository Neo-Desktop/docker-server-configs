version: '2.1'

services:
    portainer:
        extends:
            file: portainer/portainer.yml
            service: portainer
    samba:
        extends:
            file: samba/samba.yml
            service: samba
    softethervpn:
        extends:
            file: softethervpn/softethervpn.yml
            service: softethervpn
    tftpd:
        extends:
            file: tftpd/tftpd.yml
            service: tftpd
    nginx:
        extends:
            file: nginx/nginx.yml
            service: nginx
