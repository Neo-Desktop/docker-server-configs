version: '2.1'

services:
    portainer:
        extends:
            file: portainer/portainer.yaml
            service: portainer
    samba:
        extends:
            file: samba/samba.yaml
            service: samba
    softethervpn:
        extends:
            file: softethervpn/softethervpn.yaml
            service: softethervpn
    tftpd:
        extends:
            file: tftpd/tftpd.yaml
            service: tftpd
    nginx:
        extends:
            file: nginx/nginx.yaml
            service: nginx
